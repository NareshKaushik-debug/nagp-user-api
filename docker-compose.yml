version: '3'
services:
  mongo:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"
  user-api:
    image: nareshkaushik36/nagp-user-api:0.1
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/users
    depends_on:
      - mongo